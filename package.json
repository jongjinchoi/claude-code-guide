{
  "name": "claude-code-guide-rebuild",
  "version": "1.0.0",
  "description": "Claude Code Guide - Complete rebuild with clean architecture",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "npm run clean && vite build",
    "preview": "vite preview",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "terser": "^5.43.1",
    "vite": "^5.0.0"
  },
  "type": "module",
  "dependencies": {
    "@supabase/supabase-js": "^2.52.0"
  }
}
